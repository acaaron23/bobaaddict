const nextConfig = {
  images: {
      remotePatterns: [
          {
              protocol: 'https',
              hostname: '1h3.googleusercontent.com',
          },
      ],
  },
};

export default nextConfig;
